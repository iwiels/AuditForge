# Quickstart

1. Install Go 1.24+ and the desired security scanners.
2. Clone the repo.
3. Run `go test ./...`.
4. Audit a project directory or host.

Example:

```bash
go run ./cmd/orquestador-auditor --target ./sample-repo
```
